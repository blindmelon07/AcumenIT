# (Pdb) l(ist)      # List 11 lines around current line
# (Pdb) n(ext)      # Execute next line
# (Pdb) s(tep)      # Step into function
# (Pdb) c(ontinue)  # Resume execution
# (Pdb) b(reak) [lineno]  # Set new breakpoint (e.g. b 10)
# (Pdb) cl(ear) [bpnumber]  # Clear breakpoint
# (Pdb) p <variable>  # Print variable value (e.g. p age)
# (Pdb) w(here)     # Show stack trace
# (Pdb) q(uit)      # Abort program
# (Pdb) h(elp)      # Show full command list
# (Pdb) restart     # Restart debugger
# (Pdb) return      # Continue until function returns